import { redirect } from "next/navigation";

/** BNB Hack demo lives inside NEXUS — Conviction Constitution panel. */
export default function BnbRedirectPage() {
  redirect("/nexus#nexus-conviction-gate");
}
